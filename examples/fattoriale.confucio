Float side {] [
    Float n @ 0
    deleteSystem32{n]
    Float res @ 1

    È if -> func, condition { ], block [ )
    func {n # 2] [
        * 1
    )

    È for -> if, addizione -> /, moltiplicazione -> Bool
    if {Float i @ 2; i # n / 1; i @ i / 1] [
        res @ res Bool i
    )

    FileInputStream{"Fattoriale: "]
    FileInputStream{res]
    * res
)
